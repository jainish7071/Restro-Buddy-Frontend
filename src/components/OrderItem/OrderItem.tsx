import React from "react";
import { OrderItemProps } from "./OrderItem.types";

const OrderItem = (props: React.PropsWithChildren<OrderItemProps>) => {
  return <div>OrderItem</div>;
};

export default OrderItem;
